
import { View } from 'react-native';
import { UnifiedAuthForm } from '../organisms/UnifiedAuthForm';

export const UnifiedAuth = () => (
  <View style={{ flex: 1, backgroundColor: '#fff' }}>
    <UnifiedAuthForm />
  </View>
);
